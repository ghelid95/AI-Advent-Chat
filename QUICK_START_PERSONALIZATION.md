# Quick Start: Developer Personalization

## 🚀 Быстрый старт за 3 шага

### Шаг 1: Проверьте конфигурацию

В корне проекта создан файл `developer-personalization.json` с вашими настройками:

```bash
cat developer-personalization.json
```

### Шаг 2: Запустите приложение

```bash
./gradlew run
```

### Шаг 3: Протестируйте

Попробуйте запросы:

1. **Создание кода:**
   ```
   Создай новый репозиторий для работы с задачами
   ```
   → LLM учтет MVVM, Dagger 2, Coroutines

2. **Архитектурный вопрос:**
   ```
   Как лучше организовать обработку ошибок в этом модуле?
   ```
   → LLM предложит комбинированный подход с объяснением trade-offs

3. **Рефакторинг:**
   ```
   Отрефактори этот код в функциональном стиле
   ```
   → LLM учтет ваш смешанный стиль (ООП + функциональное)

## ✅ Проверка работы

Персонализация активна, если в консоли при запуске видите:
```
[ChatViewModel] Developer personalization is enabled
```

## 🔧 Быстрая настройка

### Изменить уровень детализации

В `developer-personalization.json`:
```json
"responseStyle": {
  "detailLevel": "Краткие ответы"  // или "Детальные объяснения"
}
```

### Добавить новый фреймворк

```json
"tools": {
  "frameworks": [
    "Kotlin Coroutines",
    "Compose Desktop",
    "Ktor",
    "Dagger 2",
    "YourNewFramework"  // добавьте сюда
  ]
}
```

### Изменить стиль кодирования

```json
"codingStyle": {
  "programmingParadigm": "Функциональный стиль"  // вместо "Смешанный стиль"
}
```

После изменений перезапустите приложение.

## 🎯 Примеры использования

### Пример 1: Новая фича

**Запрос:**
```
Добавь возможность фильтрации задач по статусу
```

**С персонализацией LLM создаст:**
- ViewModel для MVVM
- Использование Coroutines для асинхронности
- Dagger 2 для DI
- Integration тесты
- KDoc для public API
- Модульная структура

### Пример 2: Code Review

**Запрос:**
```
Посмотри на этот код и предложи улучшения
[ваш код]
```

**С персонализацией LLM проверит:**
- Соответствие MVVM
- Применение DRY, KISS, YAGNI
- Правильное использование Coroutines
- Наличие KDoc для public API
- Naming conventions

### Пример 3: Решение проблемы

**Запрос:**
```
У меня ошибка в асинхронном коде, помоги исправить
[код с ошибкой]
```

**С персонализацией LLM предложит:**
- Решение с Coroutines (ваш предпочитаемый подход)
- Комбинированный подход к обработке ошибок
- Объяснение trade-offs разных решений

## 📚 Дополнительная информация

- **Полная документация**: [PERSONALIZATION.md](PERSONALIZATION.md)
- **Детальное описание**: [PERSONALIZATION_SUMMARY.md](PERSONALIZATION_SUMMARY.md)
- **Основной README**: [README.md](README.md)

## 💡 Советы

1. **Специфичность**: Чем точнее настройки, тем лучше результаты
2. **Экспериментируйте**: Попробуйте разные уровни детализации
3. **Обновляйте**: По мере изменения практик обновляйте конфигурацию
4. **Комбинируйте**: Используйте с Code Assistant и Git Integration

## ❓ Частые вопросы

**Q: Как отключить персонализацию?**
A: Установите `"enabled": false` в `developer-personalization.json`

**Q: Можно ли иметь несколько профилей?**
A: Пока нет, но можно вручную менять конфигурацию для разных проектов

**Q: Нужно ли перезапускать после изменений?**
A: Да, изменения применяются при запуске приложения

**Q: Работает ли с custom system prompts?**
A: Да, персонализация добавляется к вашему custom prompt

## 🎉 Готово!

Теперь LLM понимает ваши предпочтения и будет генерировать более релевантные ответы!

Попробуйте создать что-то новое или задать вопрос - и вы увидите разницу.
