# Voice-to-Text (Speech Recognition) Guide

## Обзор

Функция Voice-to-Text позволяет записывать голосовые сообщения с микрофона и автоматически преобразовывать их в текст с помощью OpenAI Whisper API. Распознанный текст автоматически отправляется в LLM для обработки.

## Возможности

✨ **Запись с микрофона** - записывает аудио в высоком качестве (16kHz, 16-bit, mono)
✨ **Распознавание речи** - использует OpenAI Whisper API для точного распознавания
✨ **Поддержка языков** - автоматическое определение языка или ручная настройка
✨ **Визуальная индикация** - показывает статус записи и транскрибации
✨ **Автоотправка** - распознанный текст автоматически отправляется в чат

## Настройка

### 1. Установите API ключ OpenAI

Voice-to-Text использует OpenAI Whisper API, поэтому требуется API ключ OpenAI:

**Windows (PowerShell):**
```powershell
$env:OPENAI_API_KEY="your-openai-api-key-here"
```

**macOS/Linux (Bash):**
```bash
export OPENAI_API_KEY="your-openai-api-key-here"
```

**Постоянная настройка:**
Добавьте экспорт в ваш shell profile (`~/.bashrc`, `~/.zshrc`, или Windows Environment Variables).

### 2. Включите Voice-to-Text

В файле `~/.ai-advent-chat/app-settings.json` установите:

```json
{
  "voiceToTextEnabled": true,
  "voiceLanguage": null  // null = автоопределение, или "ru", "en", и т.д.
}
```

Или измените настройку напрямую в коде перед запуском.

### 3. Проверьте микрофон

Убедитесь, что микрофон подключен и работает:
- Windows: Параметры → Система → Звук → Ввод
- macOS: Системные настройки → Звук → Вход
- Linux: `arecord -l` для проверки доступных устройств

## Использование

### Интерфейс

После включения функции в интерфейсе чата появится кнопка микрофона рядом с кнопкой отправки:

```
[TextField] [🎤] [Send]
```

### Запись и отправка

1. **Начните запись:**
   - Нажмите на кнопку микрофона (🎤)
   - Кнопка станет красной, показывая что идет запись

2. **Говорите:**
   - Четко произнесите ваше сообщение
   - Рекомендуется говорить на расстоянии 15-30 см от микрофона

3. **Остановите запись:**
   - Нажмите на кнопку остановки (⏹️)
   - Начнется транскрибация (кнопка станет желтой ⏳)

4. **Автоматическая отправка:**
   - После завершения транскрибации текст автоматически отправится в LLM
   - Распознанный текст будет виден в истории чата

### Индикаторы состояния

Кнопка микрофона меняет цвет и иконку в зависимости от состояния:

| Состояние | Цвет | Иконка | Описание |
|-----------|------|--------|----------|
| Готово | Серый | 🎤 | Готов к записи |
| Запись | Красный | ⏹️ | Идет запись аудио |
| Транскрибация | Желтый | ⏳ | Преобразование речи в текст |

## Настройка языка

### Автоматическое определение (по умолчанию)

При настройке `"voiceLanguage": null` Whisper автоматически определит язык речи:

```json
{
  "voiceLanguage": null
}
```

### Указание конкретного языка

Для более точного распознавания можно указать конкретный язык:

```json
{
  "voiceLanguage": "ru"  // для русского
}
```

**Популярные языковые коды:**
- `"en"` - English
- `"ru"` - Русский
- `"es"` - Español
- `"fr"` - Français
- `"de"` - Deutsch
- `"it"` - Italiano
- `"pt"` - Português
- `"zh"` - 中文 (Chinese)
- `"ja"` - 日本語 (Japanese)
- `"ko"` - 한국어 (Korean)

## Технические детали

### Формат аудио

Записывается в формате WAV с параметрами:
- **Sample Rate**: 16kHz (оптимально для речи)
- **Bit Depth**: 16-bit
- **Channels**: Mono (1 канал)
- **Encoding**: PCM (без сжатия)

### API использование

**Endpoint:** `https://api.openai.com/v1/audio/transcriptions`

**Model:** `whisper-1` (OpenAI Whisper)

**Timeout:** 60 секунд (достаточно для большинства сообщений)

### Стоимость

OpenAI Whisper API:
- **$0.006 за минуту** аудио
- Округление до ближайшей секунды

Примеры:
- 10 секунд записи ≈ $0.001
- 30 секунд записи ≈ $0.003
- 1 минута записи = $0.006

## Примеры использования

### Пример 1: Простой вопрос

1. Нажмите кнопку микрофона
2. Скажите: "Как создать новый репозиторий в Kotlin?"
3. Нажмите остановку
4. Whisper распознает текст и отправит его в LLM

### Пример 2: Длинное описание

1. Начните запись
2. Опишите проблему детально (до 1-2 минут)
3. Остановите запись
4. Получите распознанный текст и ответ от LLM

### Пример 3: Команды

Голосом можно отправлять и команды:

1. Запись: "/help"
2. Распознанный текст: "/help"
3. Выполнится команда help

## Устранение неполадок

### Ошибка: "OPENAI_API_KEY not set"

**Решение:**
1. Убедитесь, что переменная окружения установлена
2. Перезапустите приложение после установки
3. Проверьте: `echo $OPENAI_API_KEY` (Linux/Mac) или `echo %OPENAI_API_KEY%` (Windows)

### Ошибка: "Microphone not available"

**Решение:**
1. Проверьте подключение микрофона
2. Убедитесь, что микрофон не используется другим приложением
3. Проверьте права доступа к микрофону в системных настройках
4. Попробуйте другой микрофон (если доступен)

### Плохое качество распознавания

**Решения:**
1. Говорите четко и не спешите
2. Уменьшите фоновый шум
3. Используйте качественный микрофон
4. Укажите конкретный язык вместо автоопределения
5. Проверьте уровень громкости микрофона

### Медленная транскрибация

**Причины:**
1. Длинная аудиозапись (>1 минуты)
2. Медленное интернет-соединение
3. Высокая нагрузка на OpenAI API

**Решения:**
1. Записывайте короткие сообщения (до 30 секунд)
2. Проверьте скорость интернета
3. Подождите и повторите попытку

### Ошибка транскрибации

**Решение:**
1. Проверьте API ключ OpenAI
2. Убедитесь в наличии средств на аккаунте OpenAI
3. Проверьте лимиты API (rate limits)
4. Попробуйте позже если API недоступен

## Альтернативы

Если OpenAI Whisper API недоступен или дорог, можно рассмотреть:

### Локальное решение (будущее улучшение)

Использование локального Whisper через:
- **whisper.cpp** - C++ реализация Whisper
- **faster-whisper** - оптимизированная Python версия
- **Ollama** - если добавят поддержку Whisper

### Другие API

- **Google Speech-to-Text**
- **Microsoft Azure Speech**
- **AssemblyAI**

## Конфигурация в коде

### Структура данных

```kotlin
data class AppSettings(
    // ...
    val voiceToTextEnabled: Boolean = false,
    val voiceLanguage: String? = null
)

data class ChatUiState(
    // ...
    val voiceToTextEnabled: Boolean = false,
    val isRecording: Boolean = false,
    val isTranscribing: Boolean = false,
    val voiceLanguage: String? = null
)
```

### Основные компоненты

1. **AudioRecordingService** - запись аудио с микрофона
2. **WhisperClient** - интеграция с OpenAI Whisper API
3. **ChatViewModel** - управление состоянием и процессом
4. **App.kt** - UI компонент (кнопка микрофона)

## Безопасность и приватность

🔒 **Локальная запись** - аудио записывается только на вашем компьютере

🔒 **Временные файлы** - аудиофайлы автоматически удаляются после транскрибации

🔒 **API передача** - аудио отправляется только в OpenAI для транскрибации

⚠️ **OpenAI политика** - OpenAI не использует данные API для обучения моделей (см. [Privacy Policy](https://openai.com/policies/privacy-policy))

## Советы для лучшего распознавания

1. **Качество микрофона**
   - Используйте внешний USB микрофон для лучшего качества
   - Избегайте встроенных микрофонов ноутбука в шумной среде

2. **Окружение**
   - Записывайте в тихом помещении
   - Закройте окна (внешний шум)
   - Выключите вентиляторы и кондиционеры

3. **Техника речи**
   - Говорите четко и разборчиво
   - Держите постоянную громкость
   - Делайте паузы между предложениями
   - Избегайте междометий ("эээ", "ааа")

4. **Длина записи**
   - Оптимально: 10-30 секунд
   - Максимально: до 2 минут (но будет дольше обрабатываться)
   - Длинные сообщения лучше разбивать на части

## Интеграция с другими функциями

Voice-to-Text работает совместно со всеми функциями приложения:

✅ **Developer Personalization** - распознанный текст учитывает ваши предпочтения
✅ **Code Assistant** - можно голосом задавать вопросы о коде
✅ **Git Integration** - голосовые команды для git операций
✅ **Chat Commands** - можно произносить команды голосом
✅ **MCP Tools** - распознанный текст может вызывать MCP инструменты

## FAQ

**Q: Можно ли использовать без интернета?**
A: Нет, требуется подключение к OpenAI API. В будущем планируется локальное решение.

**Q: Поддерживаются ли все языки?**
A: Whisper поддерживает 99+ языков. Полный список: [Whisper Languages](https://github.com/openai/whisper#available-models-and-languages)

**Q: Какая точность распознавания?**
A: Whisper обеспечивает точность 90-95% для четкой речи на популярных языках.

**Q: Можно ли редактировать текст перед отправкой?**
A: В текущей версии текст отправляется автоматически. Можно добавить предпросмотр в будущих версиях.

**Q: Есть ли ограничения на длину записи?**
A: OpenAI Whisper API поддерживает файлы до 25MB. Практически это ~2-3 часа аудио.

**Q: Работает ли офлайн?**
A: Нет, требуется интернет для отправки аудио в OpenAI API.

## Известные ограничения

1. **Требуется интернет** - нет офлайн режима
2. **Только OpenAI** - пока нет других провайдеров
3. **Автоотправка** - нельзя редактировать перед отправкой
4. **Моно запись** - нет поддержки стерео
5. **Один язык** - нельзя менять язык во время записи

## Roadmap

Планируемые улучшения:

- [ ] Локальное распознавание с Whisper.cpp
- [ ] Предпросмотр текста перед отправкой
- [ ] Поддержка нескольких языков в одной записи
- [ ] Настройка горячих клавиш (PTT - Push-to-Talk)
- [ ] Индикатор уровня громкости
- [ ] Выбор устройства ввода (если несколько микрофонов)
- [ ] Сохранение истории записей
- [ ] Экспорт аудио файлов

## Ссылки

- [OpenAI Whisper](https://openai.com/research/whisper)
- [Whisper API Docs](https://platform.openai.com/docs/guides/speech-to-text)
- [OpenAI Pricing](https://openai.com/pricing)

---

**Версия**: 1.0.0
**Дата обновления**: January 2026
